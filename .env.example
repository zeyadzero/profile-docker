# Copy this file to .env and fill in real values.
# .env itself must NEVER be committed (see .gitignore).

DB_NAME=accounts
DB_USER=admin
DB_PASS=change_me_strong_password
DB_ROOT_PASS=change_me_root_password

MQ_USER=admin
MQ_PASS=change_me_mq_password
